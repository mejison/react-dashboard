import React from "react";

export const Strategy = ({ colorActive }) => {
  return <div style={{ color: `${colorActive}` }}>Strategy</div>;
};

export const Menu1 = ({ colorActive }) => {
  return <div style={{ color: `${colorActive}` }}>Menu 1</div>;
};

export const Menu2 = ({ colorActive }) => {
  return <div style={{ color: `${colorActive}` }}>Menu 2</div>;
};

export const Menu3 = ({ colorActive }) => {
  return <div style={{ color: `${colorActive}` }}>Menu 3</div>;
};
